import React from 'react';
import Logo from '../assets/logo.jpg';

const Header = () => {

    return (
        <header>
            <img src={Logo} alt='Logo'/>
        </header>
    )
}

export default Header;